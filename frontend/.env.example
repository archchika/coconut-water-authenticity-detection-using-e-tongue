# Phase 6.1 / 6.8 — Frontend env (optional). No secrets in repo.
# Copy to .env.local. Only VITE_* are exposed to the client.

# Django API base URL (no trailing slash). Dev: leave unset to use Vite proxy (/api → backend).
# VITE_API_BASE_URL=http://localhost:8000
